const Joi = require('joi')

module.exports = {
  <% if(!config.database.isMongoDB) { -%>
    id: Joi.string().guid(),
  <% } -%>
    
  name: Joi
    .string()
    .min(1)
    .max(120)
    .trim()
    .required()
    .label('Name')
};
